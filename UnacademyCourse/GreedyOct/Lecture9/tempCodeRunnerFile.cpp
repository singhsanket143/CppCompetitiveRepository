for(int i = 0 ; i < buckets.size(); i++) {
    //        if(buckets[i].empty()) continue;
    //        for(int idx : buckets[i]) {
    //            bi[idx] = curr;
    //        }
    //        curr++;
    //    }